# Local development only — Netlify builds use requirements.txt (keeps CI lean).
-r requirements.txt
jupyterlab>=4,<5
# Packages used by the notebook content, for running them in local JupyterLab.
# numpy / matplotlib / pandas are provided in-browser by Pyodide, so they stay here.
# ipywidgets is also in requirements.txt because it ships a frontend extension that
# JupyterLite must bundle into the deployed site (see comment there).
numpy
matplotlib
pandas
ipywidgets
ipympl
